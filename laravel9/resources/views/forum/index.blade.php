@extends('layouts.app') @section('title','Форум') @section('page-title','Форум') @section('content')<div class="row g-4"><div class="col-lg-8"><div class="panel"><h2>Обсуждения</h2>@forelse($topics as $topic)<article class="forum-topic"><div><strong>{{ $topic->title }}</strong><p>{{ \Illuminate\Support\Str::limit($topic->body,180) }}</p></div><small>{{ $topic->author?->full_name }} · {{ $topic->created_at->format('d.m.Y H:i') }}</small></article>@empty<p class="empty">Тем пока нет.</p>@endforelse {{ $topics->links() }}</div></div><div class="col-lg-4"><div class="panel sticky-panel"><h2>Новая тема</h2><form method="post" action="{{ route('forum.store') }}">@csrf<input class="form-control mb-2" name="title" placeholder="Заголовок" required><textarea class="form-control mb-2" rows="6" name="body" placeholder="Сообщение" required></textarea><button class="btn btn-primary w-100">Опубликовать</button></form></div></div></div>@endsection
