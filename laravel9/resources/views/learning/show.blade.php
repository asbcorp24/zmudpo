@extends('layouts.app') @section('title',$section->title) @section('page-title','Учебный раздел') @section('content')<div class="page-heading"><div><a class="back-link" href="{{ route('learning.index') }}">← Все разделы</a><h1>{{ $section->title }}</h1><p>{{ $section->description }}</p></div><div class="badge-soft">{{ number_format($progress->progress_percent,0) }}% пройдено</div></div><div class="panel">@forelse($section->contents as $item)<article class="content-item"><div class="content-icon">{{ strtoupper(substr($item->type,0,2)) }}</div><div><h3>{{ $item->title }}</h3>@if($item->body)<div class="content-body">{!! $item->body !!}</div>@endif @if($item->file_path)<a href="{{ Storage::url($item->file_path) }}">Скачать материал</a>@endif @if($item->external_url)<a target="_blank" rel="noopener" href="{{ $item->external_url }}">Открыть ресурс ↗</a>@endif</div></article>@empty<p class="empty">Материалы пока не добавлены.</p>@endforelse</div><form class="mt-3" method="post" action="{{ route('learning.complete',$section) }}">@csrf<button class="btn btn-success">Отметить раздел пройденным</button></form>@endsection
