@extends('layouts.app') @section('title','Обзор') @section('page-title','Обзор обучения') @section('content')<div class="hero-panel"><div><span class="eyebrow">Личный кабинет</span><h1>{{ $user->full_name }}</h1><p>Здесь собраны текущие программы, прогресс, тесты и важные объявления.</p></div><div class="progress-ring"><strong>{{ number_format($avgProgress,0) }}%</strong><span>средний прогресс</span></div></div><div class="stats-grid"><div class="stat-card"><span>Программ</span><strong>{{ $enrollments->count() }}</strong></div><div class="stat-card"><span>Активных</span><strong>{{ $enrollments->where('status','active')->count() }}</strong></div><div class="stat-card"><span>Последних тестов</span><strong>{{ $lastAttempts->count() }}</strong></div></div><div class="row g-4 mt-1"><div class="col-lg-7"><div class="panel"><div class="panel-head"><h2>Мои программы</h2><a href="{{ route('learning.index') }}">Перейти к обучению</a></div>@forelse($enrollments as $e)<div class="list-row"><div><strong>{{ $e->program->title }}</strong><small>{{ $e->group?->name ?: 'Без группы' }}</small></div><span class="status {{ $e->status }}">{{ $e->status }}</span></div>@empty<p class="empty">Нет назначенных программ.</p>@endforelse</div></div><div class="col-lg-5"><div class="panel"><div class="panel-head"><h2>Объявления</h2></div>@forelse($announcements as $a)<article class="announcement"><strong>{{ $a->title }}</strong><p>{{ \Illuminate\Support\Str::limit(strip_tags($a->body),120) }}</p><small>{{ optional($a->published_at)->format('d.m.Y') }}</small></article>@empty<p class="empty">Новых объявлений нет.</p>@endforelse</div></div></div>@endsection
