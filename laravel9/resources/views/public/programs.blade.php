@extends('layouts.public')
@section('title','Программы обучения')
@section('content')
<section class="detail-hero"><div class="container"><span class="hero-kicker">Каталог обучения</span><h1 class="display-4 fw-bold mt-3">Программы и циклы</h1><p class="lead text-secondary">ДПО, НМО и другие форматы дополнительного профессионального образования.</p></div></section>
<section class="section-block"><div class="container"><form class="filter-bar mb-4" method="get"><div class="row g-3"><div class="col-lg-5"><label class="form-label">Поиск</label><input class="form-control" name="q" value="{{request('q')}}" placeholder="Название или направление"></div><div class="col-lg-3"><label class="form-label">Категория</label><select class="form-select" name="category"><option value="">Все категории</option>@foreach($categories as $c)<option @selected(request('category')===$c) value="{{$c}}">{{$c}}</option>@endforeach</select></div><div class="col-lg-2"><label class="form-label">Формат</label><select class="form-select" name="mode"><option value="">Все</option><option @selected(request('mode')==='dpo') value="dpo">ДПО</option><option @selected(request('mode')==='nmo') value="nmo">НМО</option></select></div><div class="col-lg-2 d-flex align-items-end"><button class="btn btn-primary w-100">Найти</button></div></div></form><div class="row g-4">@forelse($programs as $p)<div class="col-md-6 col-xl-4"><article class="course-card"><a href="{{route('public.program',$p)}}"><div class="course-image">@if($p->image)<img src="{{asset('timg/'.$p->image)}}" alt="{{$p->title}}">@else<span class="display-5">✚</span>@endif</div><div class="course-body"><div class="course-meta">@if($p->category)<span class="pill">{{$p->category}}</span>@endif<span class="pill">{{strtoupper($p->mode)}}</span>@if($p->hours)<span class="pill">{{$p->hours}} ч.</span>@endif</div><h3>{{$p->title}}</h3><div class="d-flex justify-content-between align-items-center mt-3"><span class="text-secondary">{{$p->starts_at?->format('d.m.Y')??'По набору'}}</span>@if($p->price!==null)<strong>{{number_format((float)$p->price,0,',',' ')}} ₽</strong>@endif</div></div></a></article></div>@empty<div class="col-12"><div class="news-card">По выбранным условиям программ не найдено.</div></div>@endforelse</div><div class="mt-4">{{$programs->links()}}</div></div></section>
@endsection
