@extends('layouts.app') @section('title','Объявления') @section('content') @include('curator._legacy_nav')
<div class="page-heading"><div><h1>Объявления</h1><p>Аналог kurator_obiav.php.</p></div></div><div class="panel"><h2>Новое объявление</h2><form method="post" action="{{route('curator.announcements.store')}}">@csrf<div class="grid"><div><label>Программа</label><select name="program_id" required>@foreach($programs as $p)<option value="{{$p->id}}">{{$p->title}}</option>@endforeach</select></div><div><label>Заголовок</label><input name="title" required></div><div><label>Дата публикации</label><input type="datetime-local" name="published_at"></div></div><label>Текст</label><textarea name="body" required></textarea><button class="btn">Опубликовать</button></form></div>
<div class="panel mt-3"><h2>Опубликованные</h2>@forelse($items as $a)<div class="list-row"><div><strong>{{$a->title}}</strong><small>{{$a->program?->title??'Общее'}} · {{$a->published_at?->format('d.m.Y H:i')}}</small><div>{{$a->body}}</div></div><span class="status">{{$a->is_active?'активно':'скрыто'}}</span></div>@empty<p class="empty">Объявлений нет.</p>@endforelse{{$items->links()}}</div>@endsection