@extends('layouts.app') @section('title','Файлы студентов') @section('content') @include('curator._legacy_nav')
<div class="page-heading"><div><h1>Проверка файлов студентов</h1><p>Файлы практических и итоговых работ, как в kurator_prov_file.php.</p></div></div>
<div class="panel"><h2>Практические работы</h2>@forelse($submissions as $s)<div class="list-row"><div><strong>{{$s->user?->full_name}} — {{$s->practiceAssignment?->title}}</strong><small>{{$s->submitted_at?->format('d.m.Y H:i')}} · {{$s->status}}</small></div><a class="btn btn-sm" href="{{route('practice.download',$s)}}">Скачать</a></div>@empty<p class="empty">Файлов нет.</p>@endforelse</div>
<div class="panel mt-3"><h2>Итоговые работы</h2>@forelse($finals as $f)<div class="list-row"><div><strong>{{$f->user?->full_name}} — {{$f->enrollment?->program?->title}}</strong><small>{{$f->submitted_at?->format('d.m.Y H:i')}} · {{$f->status}}</small></div><a class="btn btn-sm" href="{{route('final-works.download',$f)}}">Скачать</a></div>@empty<p class="empty">Файлов нет.</p>@endforelse</div>@endsection