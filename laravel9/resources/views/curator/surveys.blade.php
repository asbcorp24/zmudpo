@extends('layouts.app') @section('title','Анкеты') @section('content') @include('curator._legacy_nav')
<div class="page-heading"><div><h1>Анкетные тесты и анкеты</h1><p>Объединяет legacy kurator_ank.php и kurator_ank_dan.php.</p></div></div>
@forelse($surveys as $survey)<div class="panel"><h2>{{$survey->title}}</h2><div class="table-responsive"><table class="table"><tr><th>Слушатель</th>@foreach($survey->fields as $f)<th>{{$f->label}}</th>@endforeach</tr>@foreach($students as $uid=>$student)<tr><td>{{$student->full_name}}</td>@foreach($survey->fields as $f) @php($ans=($responses[$uid]??collect())->firstWhere('survey_field_id',$f->id)) <td>{{$ans?->value??'—'}}</td>@endforeach</tr>@endforeach</table></div></div>@empty<div class="panel empty">Анкет нет.</div>@endforelse @endsection