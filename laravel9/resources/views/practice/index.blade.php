@extends('layouts.app') @section('title','Практика') @section('page-title','Практика и контрольные работы') @section('content')<div class="page-heading"><div><h1>Практические задания</h1><p>Дневники практики, контрольные и итоговые работы с проверкой куратором.</p></div></div>@forelse($assignments as $a) @php($submission=$submissions->get($a->id)) <div class="panel assignment"><div><span class="course-type">{{ $submission?->status ?: 'не отправлено' }}</span><h3>{{ $a->title }}</h3><p>{{ $a->description }}</p>@if($a->ends_at)<small>Срок: {{ $a->ends_at->format('d.m.Y') }}</small>@endif @if($submission?->review_comment)<div class="review-note"><strong>Комментарий куратора:</strong> {{ $submission->review_comment }}</div>@endif</div><form method="post" enctype="multipart/form-data" action="{{ route('practice.submit',$a) }}">@csrf<input class="form-control mb-2" type="file" name="file"><textarea class="form-control mb-2" name="comment" placeholder="Комментарий к работе"></textarea><button class="btn btn-primary">{{ $submission?'Отправить новую версию':'Отправить работу' }}</button></form></div>@empty<div class="panel empty">Практические задания не назначены.</div>@endforelse @endsection
