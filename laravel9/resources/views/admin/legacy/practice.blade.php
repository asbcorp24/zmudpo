@extends('layouts.app') @section('content')<h1>Циклы практики</h1><form method="post" class="card card-body mb-3">@csrf<input class="form-control mb-2" name="title" placeholder="Название цикла"><select class="form-select mb-2" name="program_id"><option value="">Без программы</option>@foreach($programs as $p)<option value="{{$p->id}}">{{$p->title}}</option>@endforeach</select><div class="row"><div class="col"><input type="date" class="form-control" name="starts_at"></div><div class="col"><input type="date" class="form-control" name="ends_at"></div></div><button class="btn btn-primary mt-2">Создать цикл</button></form>@foreach($cycles as $c)<div class="card mb-3"><div class="card-header"><b>{{$c->title}}</b></div><div class="card-body"><ul>@foreach($c->tasks as $t)<li>{{$t->title}} — {{$t->response_type}}</li>@endforeach</ul><form method="post">@csrf<input type="hidden" name="entity" value="task"><input type="hidden" name="practice_cycle_id" value="{{$c->id}}"><div class="row g-2"><div class="col"><input class="form-control" name="title" placeholder="Задание"></div><div class="col"><select class="form-select" name="response_type">@foreach(['text','file','screenshot','text_file','text_screenshot','file_screenshot','all'] as $v)<option>{{$v}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Добавить задание</button></div></div></form></div></div>@endforeach@endsection