@extends('layouts.app') @section('content')<h1>Архив обучения</h1><form method="post" enctype="multipart/form-data" action="{{route('admin.legacy.archive-import')}}" class="card card-body">@csrf<label>Legacy XML архива</label><input type="file" name="file" accept=".xml" required><button class="btn mt-2">Импортировать снимок</button></form><table class="table"><tr><th>Тип</th><th>Название</th><th>Период</th><th>Данные</th></tr>@foreach($items as $x)<tr><td>{{$x->type}}</td><td>{{$x->title}}</td><td>{{$x->started_at}} — {{$x->ended_at}}</td><td><small>{{json_encode($x->data,JSON_UNESCAPED_UNICODE)}}</small></td></tr>@endforeach</table>{{$items->links()}}@endsection