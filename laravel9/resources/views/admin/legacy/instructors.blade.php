@extends('layouts.app') @section('content')<h1>Преподаватели и расписание НМО</h1><form method="post" class="card card-body mb-3">@csrf<div class="row g-2"><div class="col"><input class="form-control" name="full_name" placeholder="ФИО"></div><div class="col"><input class="form-control" name="email" placeholder="Email"></div><div class="col"><select class="form-select" name="program_id"><option value="">Везде</option>@foreach($programs as $p)<option value="{{$p->id}}">{{$p->title}}</option>@endforeach</select></div><div class="col"><input class="form-control" name="subject" placeholder="Предмет"></div><div class="col-auto"><button class="btn btn-primary">Добавить</button></div></div></form>@foreach($items as $i)<div class="card mb-3"><div class="card-header"><b>{{$i->full_name}}</b></div><div class="card-body">@foreach($i->assignments as $a)<div class="mb-2">{{$a->program?->title ?? 'Все программы'}} / {{$a->subject}}<form method="post" action="{{route('admin.legacy.instructor-slot')}}" class="row g-1 mt-1">@csrf<input type="hidden" name="instructor_program_id" value="{{$a->id}}"><div class="col"><input type="date" class="form-control" name="date"></div><div class="col"><input type="time" class="form-control" name="time"></div><div class="col"><input type="number" class="form-control" name="capacity" placeholder="мест"></div><div class="col"><input type="number" class="form-control" name="lesson_number" placeholder="занятие"></div><div class="col-auto"><button class="btn btn-outline-primary">Слот</button></div></form></div>@endforeach</div></div>@endforeach@endsection