@extends('layouts.app') @section('content')<h1>Анкета: {{$survey->title}}</h1><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>Слушатель</th>@foreach($survey->fields as $f)<th>{{$f->title}}</th>@endforeach</tr></thead><tbody>@foreach($users as $u)<tr><td>{{$u->full_name}}</td>@foreach($survey->fields as $f)@php($x=$responses->get($u->id.'-'.$f->id))<td>{{$x?->value ?: $x?->file_path}}</td>@endforeach</tr>@endforeach</tbody></table></div><button onclick="window.print()" class="btn btn-secondary">Печать</button>@endsection