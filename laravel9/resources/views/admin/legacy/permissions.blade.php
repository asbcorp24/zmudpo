@extends('layouts.app') @section('content')<h1>Права администраторов</h1><div class="grid"><div class="card"><h3>Новое право</h3><form method="post">@csrf<input type="hidden" name="action" value="permission"><label>Ключ</label><input name="key" required placeholder="reports.view"><label>Название</label><input name="name" required><label>Группа</label><input name="group"><button class="btn">Добавить</button></form></div><div class="card"><h3>Назначение</h3><form method="post">@csrf<input type="hidden" name="action" value="assign"><select name="user_id" required>@foreach($admins as $a)<option value="{{$a->id}}">{{$a->full_name}}</option>@endforeach</select>@foreach($permissions as $p)<label><input style="width:auto" type="checkbox" name="permissions[]" value="{{$p->id}}"> {{$p->name}} <small>{{$p->key}}</small></label>@endforeach<button class="btn">Сохранить</button></form></div></div>@endsection