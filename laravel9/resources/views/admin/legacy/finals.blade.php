@extends('layouts.app') @section('content')<h1>Промежуточные и итоговые работы</h1><form method="post" class="card card-body mb-3">@csrf<div class="row g-2"><div class="col"><input class="form-control" name="title" placeholder="Вид работы"></div><div class="col"><select class="form-select" name="level"><option value="intermediate">Промежуточная</option><option value="final">Итоговая</option></select></div><div class="col"><select class="form-select" name="program_id"><option value="">Без привязки</option>@foreach($programs as $p)<option value="{{$p->id}}">{{$p->title}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary">Создать</button></div></div></form>@foreach($definitions as $d)<div class="card mb-2"><div class="card-header"><b>{{$d->title}}</b> — {{$d->level}}</div><div class="card-body"><ul>@foreach($d->themes as $t)<li>{{$t->title}}</li>@endforeach</ul><form method="post">@csrf<input type="hidden" name="entity" value="theme"><input type="hidden" name="definition_id" value="{{$d->id}}"><div class="input-group"><input class="form-control" name="title" placeholder="Тема"><button class="btn btn-outline-primary">Добавить тему</button></div></form></div></div>@endforeach@endsection