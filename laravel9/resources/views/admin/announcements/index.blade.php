@extends('layouts.app') @section('title','Объявления') @section('content')<div class="card"><form method="post" action="{{ route('admin.announcements.store') }}">@csrf<label>Заголовок</label><input name="title" required><label>Текст</label><textarea name="body" rows="5" required></textarea><label><input style="width:auto" type="checkbox" name="is_active" value="1" checked> Опубликовать</label><button class="btn">Добавить</button></form></div><div class="card">@foreach($items as $x)<div style="padding:12px 0;border-bottom:1px solid #eee"><b>{{ $x->title }}</b> <small>{{ $x->is_active?'опубликовано':'черновик' }}</small><div>{{ $x->body }}</div><form method="post" action="{{ route('admin.announcements.destroy',$x) }}">@csrf @method('DELETE')<button class="btn red">Удалить</button></form></div>@endforeach{{ $items->links() }}</div>@endsection