<!doctype html><html lang="ru"><head><meta charset="utf-8"><title>Успеваемость — {{$program->title}}</title><style>body{font-family:Arial,sans-serif;color:#111}h1{font-size:20px}table{border-collapse:collapse;width:100%;font-size:12px}th,td{border:1px solid #555;padding:5px;text-align:center}th:first-child,td:first-child{text-align:left}@media print{.no-print{display:none}}</style></head><body><div class="no-print"><button onclick="window.print()">Печать</button></div><h1>{{$program->title}}</h1><p>Отчёт по успеваемости — {{$format==='grades'?'оценки':'проценты'}}; {{ $mode==='best'?'лучшая попытка':'последняя попытка' }}.</p><table><tr><th>Слушатель</th>@foreach($assignments as $a)<th>{{$a->title}}</th>@endforeach<th>Итог</th></tr>@foreach($rows as $row)<tr><td>{{$row['user']->full_name}}</td>@foreach($assignments as $a)@php($cell=$row['cells'][$a->id])<td>@if($cell['attempt']){{$format==='grades'?$cell['grade']:(rtrim(rtrim(number_format($cell['percent'],2,'.',''),'0'),'.').'%')}}@else — @endif</td>@endforeach<td>@if($row['average']!==null){{$format==='grades'?$row['grade']:$row['average'].'%'}}@else — @endif</td></tr>@endforeach</table><p>Порог оценок: 3 — {{$thresholds[3]}}%, 4 — {{$thresholds[4]}}%, 5 — {{$thresholds[5]}}%.</p></body></html>
