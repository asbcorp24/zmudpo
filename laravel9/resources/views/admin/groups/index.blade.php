@extends('layouts.app') @section('title','Группы') @section('content')<div class="card"><form method="post" action="{{ route('admin.groups.store') }}">@csrf<label>Новая группа</label><div class="grid"><input name="name" required><button class="btn">Добавить</button></div></form></div><div class="card"><table><tr><th>Группа</th><th>Слушателей</th><th></th></tr>@foreach($items as $x)<tr><td><form method="post" action="{{ route('admin.groups.update',$x) }}">@csrf @method('PUT')<div class="grid"><input name="name" value="{{ $x->name }}"><button class="btn gray">Сохранить</button></div></form></td><td>{{ $x->enrollments_count }}</td><td><form method="post" action="{{ route('admin.groups.destroy',$x) }}">@csrf @method('DELETE')<button class="btn red">Удалить</button></form></td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection