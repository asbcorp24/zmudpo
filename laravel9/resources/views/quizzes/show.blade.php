@extends('layouts.app') @section('title',$quiz->title) @section('page-title','Тестирование') @section('content')<div class="page-heading"><div><a class="back-link" href="{{ route('quizzes.index') }}">← К тестам</a><h1>{{ $quiz->title }}</h1><p>Для успешного прохождения необходимо набрать не менее {{ $quiz->pass_percent }}%.</p></div></div><form method="post" action="{{ route('quizzes.submit',$quiz) }}">@csrf @foreach($quiz->questions as $question)<div class="panel question"><div class="question-number">Вопрос {{ $loop->iteration }}</div><h3>{{ $question->question }}</h3>@foreach($question->options as $option)<label class="answer"><input type="{{ $question->type==='multiple'?'checkbox':'radio' }}" name="answers[{{ $question->id }}][]" value="{{ $option->id }}"><span>{{ $option->text }}</span></label>@endforeach</div>@endforeach<button class="btn btn-primary btn-lg">Завершить тест</button></form>@endsection
